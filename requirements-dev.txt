# Development requirements (tools for linting, formatting, typing, and testing)
# Pin versions where reasonable; keep separate from runtime `requirements.txt`.
-r requirements.txt

black==23.3.0
flake8==5.0.4
mypy==1.4.1
pytest==7.4.0
pytest-cov==4.1.0

# Optional extras
types-requests==2.28.11.0
types-PyYAML==6.0.12.0
